---
Status: Active
Owner: HyperFleet Team
Last Updated: 2026-06-12
---

# HyperFleet v0.2.0 → v1.0.0 Upgrade Guide

This guide covers all breaking changes and required actions for operators and API consumers moving from HyperFleet v0.2.0 to v1.0.0.

> **Important:** v1.0.0 requires a fresh database. There is no data migration from v0.2.0.
> All existing v0.2.0 deployments are disposable integration environments — the upgrade is a
> **reconfigure-and-redeploy**, not an in-place migration.

**Related documentation:**

- [HYPERFLEET-1177 — Breaking-change checklist](https://redhat.atlassian.net/browse/HYPERFLEET-1177) — exhaustive verified checklist
- [HyperFleet Release Process](./hyperfleet-release-process.md) — release phases and artifacts
- [API Versioning Strategy](../../components/api-service/api-versioning.md) — versioning rules and compatibility guarantees

---

## Table of Contents

- [Breaking Changes Summary](#breaking-changes-summary)
- [1. Configuration](#1-configuration)
- [2. API Contract](#2-api-contract)
- [3. Statuses Contract](#3-statuses-contract)
- [4. Operating Model](#4-operating-model)
- [5. New Features (Informational)](#5-new-features-informational)

---

## Breaking Changes Summary

Every breaking change is listed here with a link to the detailed section. Items marked **Breaking** require action before deploying v1.0.0.

| #  | Area | Change | Severity | Detail |
|----|------|--------|----------|--------|
| C1 | Configuration | Tracing disabled by default (`HYPERFLEET_TRACING_ENABLED=false`) | Breaking | [§1.1](#11-tracing-disabled-by-default) |
| C2 | Configuration | Sentinel `message_decision` CEL block | Breaking | [§1.2](#12-sentinel-message_decision-cel-configuration) |
| C3 | Configuration | JWT `identity_claim` for audit fields | Breaking | [§1.3](#13-jwt-identity_claim-for-audit-fields) |
| C4 | Configuration | Entity types declared in config YAML | Breaking | [§1.4](#14-entity-types-declared-in-config-yaml) |
| C5 | Configuration | Condition mapping rules config block | Informational | [§1.5](#15-condition-mapping-rules) |
| A1 | API Contract | Adapter status verb: POST → PUT | Breaking | [§2.1](#21-adapter-status-reporting-post--put) |
| A2 | API Contract | Unified `Resource` response type replaces per-entity types | Breaking | [§2.2](#22-unified-resource-response-type) |
| A3 | API Contract | New endpoints: `/wifconfigs`, `/channels`, `/versions` | Informational | [§2.3](#23-new-endpoints) |
| A4 | API Contract | Delete returns 202 Accepted (soft delete) | Breaking | [§2.4](#24-delete-returns-202-accepted) |
| A5 | API Contract | `references` field on resources | Informational | [§2.5](#25-resource-references) |
| A6 | API Contract | PUT `/statuses` returns 200 only (no 201) | Breaking | [§2.6](#26-put-statuses-returns-200-only) |
| A7 | API Contract | `reason` and `message` on conditions now optional | Informational | [§2.7](#27-condition-reason-and-message-now-optional) |
| S1 | Statuses | Aggregated conditions renamed: `Ready` → `Reconciled`, `Available` → `LastKnownReconciled` | Breaking | [§3.1](#31-aggregated-condition-renames) |
| S2 | Statuses | `Finalized` condition added for deletion flows | Breaking | [§3.2](#32-finalized-condition-for-deletion-flows) |
| S3 | Statuses | `observed_time` replaces `last_updated_time` in adapter payloads | Breaking | [§3.3](#33-observed_time-replaces-last_updated_time) |
| S4 | Statuses | Resource conditions: True/False only (no Unknown) | Informational | [§3.4](#34-resource-conditions-trufalse-only) |
| S5 | Statuses | Mapped conditions (new category) | Informational | [§3.5](#35-mapped-conditions) |
| S6 | Statuses | Non-reconcilable resources have no aggregated conditions | Informational | [§3.6](#36-non-reconcilable-resources) |
| O1 | Operating Model | Fresh database — no migration from v0.2.0 | Breaking | [§4.1](#41-fresh-database--no-migration) |
| O2 | Operating Model | Descriptor-driven delete model (no `?cascade=true`) | Breaking | [§4.2](#42-descriptor-driven-delete-model) |
| O3 | Operating Model | Unknown config fields cause startup errors | Breaking | [§4.3](#43-unknown-config-fields-cause-startup-errors) |
| O4 | Operating Model | No runtime config reloading | Informational | [§4.4](#44-no-runtime-config-reloading) |

---

## 1. Configuration

### 1.1 Tracing disabled by default

#### What Changed

Tracing is now **disabled by default**. The environment variable `HYPERFLEET_TRACING_ENABLED` defaults to `false`. In v0.2.0, tracing was enabled by default.

See [Tracing Standard](../../standards/tracing.md) for the full configuration reference.

#### Required Action

If your deployment relies on tracing, explicitly enable it:

**Before (v0.2.0)** — tracing was on by default, no config needed:

```yaml
# No tracing config required — enabled by default
```

**After (v1.0.0)** — must opt in:

```yaml
tracing:
  enabled: true
```

Or via environment variable:

```bash
HYPERFLEET_TRACING_ENABLED=true
```

> **If you don't act:** Tracing data stops flowing. No functional impact — the application runs normally without traces.

---

### 1.2 Sentinel `message_decision` CEL configuration

#### What Changed

Sentinel now uses a configurable `message_decision` block with CEL expressions to decide when to publish reconciliation events. This replaces any prior hardcoded decision logic.

The default configuration references `condition("Ready")`, which must be updated to `condition("Reconciled")` due to the [aggregated condition rename](#31-aggregated-condition-renames).

See [Sentinel Design — Message Decision](../../components/sentinel/sentinel.md) for the full CEL reference.

#### Required Action

Add or update the `message_decision` block in the Sentinel ConfigMap:

**Before (v0.2.0)** — no `message_decision` block (hardcoded logic):

```yaml
# Decision logic was hardcoded
```

**After (v1.0.0)** — CEL-based configuration:

```yaml
message_decision:
  params:
    reconciled_status:
      value: condition("Reconciled").status
    reconciled_generation:
      value: condition("Reconciled").observed_generation
    current_generation:
      value: generation
  result: >-
    reconciled_status == "True"
    && reconciled_generation == current_generation
```

> **If you don't act:** Sentinel cannot evaluate whether to publish events. Reconciliation messages may not be sent.

---

### 1.3 JWT `identity_claim` for audit fields

#### What Changed

The API now supports configurable `identity_claim` for JWT tokens. This setting controls which JWT claim is extracted to populate the `created_by` and `updated_by` audit fields on resources.

#### Required Action

Configure the `identity_claim` in the API configuration:

**After (v1.0.0):**

```yaml
auth:
  identity_claim: preferred_username
```

> **If you don't act:** Audit fields (`created_by`, `updated_by`) may not be populated or may use a default claim that doesn't match your identity provider's token format.

---

### 1.4 Entity types declared in config YAML

#### What Changed

Entity types (Cluster, NodePool, WifConfig, Channel, Version) are now declared in the application's configuration YAML under an `entities` section. Each entity descriptor includes: `type`, `plural`, `nameMinLen`, `nameMaxLen`, `specSchemaName`, `requiredAdapters`, `searchDisallowedFields`, `references`, `parentType`, and `onParentDelete`.

No Go code is required for standard entity types. See [Generic Resource Registry Design](../generic-resource-registry-design.md) for the full descriptor schema.

#### Required Action

Include the `entities` section in the API config file. Example for the standard entity types:

**After (v1.0.0):**

```yaml
entities:
  - type: cluster
    plural: clusters
    name_min_len: 1
    name_max_len: 64
    spec_schema_name: ClusterSpec
    required_adapters:
      - provisioner
      - lifecycle
  - type: node_pool
    plural: nodepools
    parent_type: cluster
    on_parent_delete: cascade
    name_min_len: 1
    name_max_len: 64
    spec_schema_name: NodePoolSpec
```

> **If you don't act:** The API cannot serve any entity type. Startup will fail or no routes will be registered.

---

### 1.5 Condition mapping rules

#### What Changed

A new optional `conditions` config block under the API configuration allows operators to define CEL-based condition mapping rules. These rules copy and transform adapter-reported conditions into customer-visible resource conditions.

See [Condition Mapping Design](../../components/api-service/condition-mapping-design.md) for rule syntax and examples.

#### Required Action

**No action required** unless you want to expose provider-specific adapter conditions to API consumers. To configure:

```yaml
conditions:
  adapters:
    clusters:
      ROSAControlPlaneReady:
        when:
          expression: >-
            adapter.name == "rosa-provisioner"
            && has(condition) && condition.type == "ControlPlaneReady"
        output:
          status:
            expression: condition.status
          reason:
            expression: condition.reason
          message:
            expression: condition.message
```

---

## 2. API Contract

> **Note:** The API URI path remains `/api/hyperfleet/v1/...`. The component version bump from v0.2.0 to v1.0.0 does **not** change the API version prefix.
> See [API Versioning Strategy](../../components/api-service/api-versioning.md) for the distinction between component version and API version.

### 2.1 Adapter status reporting: POST → PUT

#### What Changed

Adapter status reporting now uses `PUT` with upsert semantics (create-or-update). The `POST` verb is no longer accepted. The API handles upsert logic server-side — adapters always send the same PUT request regardless of whether it is the first report or a subsequent update.

**Endpoint:** `PUT /api/hyperfleet/v1/clusters/{clusterId}/statuses`

See [Adapter Status Contract](../../components/adapter/framework/adapter-status-contract.md) for the full contract.

#### Required Action

Change the HTTP method in all adapter status reporting code:

**Before (v0.2.0):**

```bash
curl -X POST \
  "${API_URL}/api/hyperfleet/v1/clusters/${CLUSTER_ID}/statuses" \
  -H "Content-Type: application/json" \
  -d @status.json
```

**After (v1.0.0):**

```bash
curl -X PUT \
  "${API_URL}/api/hyperfleet/v1/clusters/${CLUSTER_ID}/statuses" \
  -H "Content-Type: application/json" \
  -d @status.json
```

> **If you don't act:** Status reports are rejected. The API returns 405 Method Not Allowed.

---

### 2.2 Unified `Resource` response type

#### What Changed

The API response type changes from per-entity types (`Cluster`, `NodePool`) to a single generic `Resource` type with a `kind` discriminator field. The `spec` field is an untyped JSON object at the API layer.

#### Required Action

Update API client code to handle the unified `Resource` response type:

**Before (v0.2.0):**

```json
{
  "id": "abc-123",
  "name": "my-cluster",
  "cloud_provider": "gcp",
  "region": "us-east1",
  "status": { ... }
}
```

**After (v1.0.0):**

```json
{
  "id": "abc-123",
  "kind": "cluster",
  "name": "my-cluster",
  "href": "/api/hyperfleet/v1/clusters/abc-123",
  "spec": {
    "cloud_provider": "gcp",
    "region": "us-east1"
  },
  "labels": {},
  "status": { ... },
  "generation": 1,
  "created_time": "2026-06-01T00:00:00Z",
  "updated_time": "2026-06-01T00:00:00Z",
  "created_by": "user@example.com",
  "updated_by": "user@example.com"
}
```

Key differences:

- Provider-specific fields move inside the `spec` object
- New fields: `kind`, `href`, `labels`, `generation`, `created_time`, `updated_time`, `created_by`, `updated_by`
- The `status` field structure is unchanged

> **If you don't act:** API client code breaks when parsing responses. Field access paths change (e.g., `response.cloud_provider` → `response.spec.cloud_provider`).

---

### 2.3 New endpoints

#### What Changed

v1.0.0 adds the following endpoints via the [Generic Resource Registry](../generic-resource-registry-design.md):

| Endpoint | Description |
|----------|-------------|
| `GET /api/hyperfleet/v1/resources` | List all registered entity types |
| `GET/POST /api/hyperfleet/v1/wifconfigs` | WIF (Workload Identity Federation) configuration resources |
| `GET/POST /api/hyperfleet/v1/channels` | Release channel resources |
| `GET/POST /api/hyperfleet/v1/versions` | Version resources |
| `POST /api/hyperfleet/v1/clusters/{id}/force-delete` | Force-delete a stuck resource (requires `Finalizing` state) |

#### Required Action

**No action required** — these are additive endpoints. Existing client code continues to work.
Adapter teams integrating with new resource types should consult the OpenAPI specification for request/response shapes.

---

### 2.4 Delete returns 202 Accepted

#### What Changed

`DELETE /api/hyperfleet/v1/clusters/{id}` now returns `202 Accepted` instead of `200 OK` or `204 No Content`. Deletion is now a two-phase process:

1. **Soft delete:** API sets `deleted_time` on the resource. Returns 202.
2. **Hard delete:** When all adapters report `Finalized=True` and no restrict-policy children remain, the API hard-deletes the records.

Attempting to delete a resource with `restrict`-policy children returns `409 Conflict`.

#### Required Action

Update client code that checks the DELETE response status code:

**Before (v0.2.0):**

```go
if resp.StatusCode != http.StatusOK {
    return fmt.Errorf("delete failed")
}
```

**After (v1.0.0):**

```go
if resp.StatusCode != http.StatusAccepted {
    return fmt.Errorf("delete failed")
}
// Resource enters Finalizing state — poll until fully deleted
```

> **If you don't act:** Delete operations appear to fail (status code mismatch) even though the soft-delete succeeded.

---

### 2.5 Resource references

#### What Changed

Resources can declare non-ownership associations via a `references` map in create and patch requests. Cardinality constraints (`min`/`max`) are enforced by the entity descriptor. Deleting a referenced resource returns `409 Conflict`.

#### Required Action

**No action required** unless you create resources that require references per the entity descriptor. If a descriptor defines required references (`min >= 1`), create requests must include the `references` field.

---

### 2.6 PUT `/statuses` returns 200 only

#### What Changed

The adapter status PUT endpoint returns `200 OK` for both first-time reports and subsequent updates. The `201 Created` response is no longer used.

#### Required Action

Update any adapter code that checks for `201` after the first status report:

**Before (v0.2.0):**

```go
if resp.StatusCode == http.StatusCreated {
    log.Info("First status report created")
}
```

**After (v1.0.0):**

```go
if resp.StatusCode == http.StatusOK {
    log.Info("Status report accepted")
}
```

> **If you don't act:** Adapter logic that branches on 201 vs 200 behaves incorrectly — all reports return 200.

---

### 2.7 Condition `reason` and `message` now optional

#### What Changed

The `reason` and `message` fields on adapter-reported conditions are now optional. They are still recommended for observability but are no longer required by the API.

#### Required Action

**No action required.** Continue providing `reason` and `message` for observability. Adapters that already provide these fields are unaffected.

---

## 3. Statuses Contract

### 3.1 Aggregated condition renames

#### What Changed

The two system-computed aggregated resource conditions were renamed:

| v0.2.0 Name | v1.0.0 Name | Meaning |
|-------------|-------------|---------|
| `Ready` | `Reconciled` | All required adapters report `Available=True` at the current generation |
| `Available` | `LastKnownReconciled` | Sticky: once `Reconciled` is `True`, this remains `True` across generation bumps until a new reconciliation completes |

The adapter-level required conditions (`Applied`, `Available`, `Health`) are **unchanged**.

See [ADR-0007 — Conditions-Based Status Model](../../adrs/0007-conditions-based-status-model.md) (Evolution section) for the decision rationale.

#### Required Action

Update all code and configuration that reads aggregated conditions from the resource status:

**Before (v0.2.0):**

```go
ready := getCondition(resource.Status.Conditions, "Ready")
available := getCondition(resource.Status.Conditions, "Available")
```

**After (v1.0.0):**

```go
reconciled := getCondition(resource.Status.Conditions, "Reconciled")
lastKnownReconciled := getCondition(resource.Status.Conditions, "LastKnownReconciled")
```

Also update Sentinel `message_decision` CEL expressions:

**Before:** `condition("Ready").status`
**After:** `condition("Reconciled").status`

> **If you don't act:** Code that reads `Ready` or `Available` from resource conditions returns nil/empty. Sentinel decision logic evaluates incorrectly, potentially blocking or duplicating reconciliation events.

---

### 3.2 `Finalized` condition for deletion flows

#### What Changed

Adapters must now report a `Finalized` condition during resource deletion flows. When `deleted_time` is set on a resource, adapters are expected to:

1. Clean up their Kubernetes/cloud resources
2. Report `Finalized=True` in their status update

The API gates hard-delete on **all** required adapters reporting `Finalized=True`. When `deleted_time` is not set, the `Finalized` condition is ignored.

#### Required Action

Add `Finalized` condition reporting to adapter deletion flow:

**After (v1.0.0):**

```json
{
  "adapter_name": "my-adapter",
  "conditions": [
    { "type": "Applied", "status": "True" },
    { "type": "Available", "status": "True" },
    { "type": "Health", "status": "True" },
    { "type": "Finalized", "status": "True", "reason": "CleanupComplete", "message": "All resources deleted" }
  ]
}
```

> **If you don't act:** Resources remain in `Finalizing` state indefinitely. The API never hard-deletes the records. Operators must use `POST /{id}/force-delete` to clear stuck resources.

---

### 3.3 `observed_time` replaces `last_updated_time`

#### What Changed

In adapter status PUT requests, the timestamp field is now `observed_time` (RFC 3339). The API uses this to set `last_report_time` on the stored adapter status.

See [Adapter Status Contract](../../components/adapter/framework/adapter-status-contract.md) for the full payload structure.

#### Required Action

Rename the field in adapter status payloads:

**Before (v0.2.0):**

```json
{
  "adapter_name": "my-adapter",
  "last_updated_time": "2026-06-01T12:00:00Z",
  "conditions": [ ... ]
}
```

**After (v1.0.0):**

```json
{
  "adapter_name": "my-adapter",
  "observed_time": "2026-06-01T12:00:00Z",
  "conditions": [ ... ]
}
```

> **If you don't act:** The API rejects the status payload or ignores the timestamp. Adapter report timing becomes unreliable.

---

### 3.4 Resource conditions: True/False only

#### What Changed

Resource-level conditions (in `status.conditions`) enforce a **True/False-only** contract. Adapter conditions may use `True`, `False`, or `Unknown`, but `Unknown` adapter conditions are automatically filtered out during condition mapping and never surface as resource conditions.

#### Required Action

**No action required.** Consumers can assume resource conditions are always `True` or `False` in steady state. `Unknown` may appear transiently during resource initialization only.

---

### 3.5 Mapped conditions

#### What Changed

In addition to the standard aggregated conditions (`Reconciled`, `LastKnownReconciled`) and per-adapter conditions, resource `status.conditions` may now contain **mapped conditions** — dynamically created from adapter conditions via operator-configured CEL mapping rules.

See [Condition Mapping Design](../../components/api-service/condition-mapping-design.md) for rule syntax.

#### Required Action

**No action required** for existing consumers. If your code iterates over `status.conditions`, be aware that new condition types may appear beyond the standard aggregated set.

---

### 3.6 Non-reconcilable resources

#### What Changed

New resource types like Channel and Version are **non-reconcilable** — they have no `Reconciled`/`LastKnownReconciled` aggregated conditions. Only reconcilable resources (Cluster, NodePool) receive aggregated conditions.

#### Required Action

**No action required** for existing adapter teams. Consumers building generic tooling should check the resource kind before expecting aggregated conditions.

---

## 4. Operating Model

### 4.1 Fresh database — no migration

#### What Changed

**v1.0.0 requires a fresh database initialization. No data migration from v0.2.0 is supported.**

The database schema changes significantly:

| v0.2.0 Tables | v1.0.0 Tables |
|----------------|----------------|
| `clusters` | `resources` (generic, all entity types) |
| `node_pools` | `resource_labels` (structured label storage) |
| `adapter_statuses` | `resource_conditions` (per-condition rows) |
| — | `resource_references` (non-ownership associations) |
| — | `adapter_statuses` (unchanged) |

All existing v0.2.0 data is discarded. This is intentional — all existing deployments are disposable integration environments owned by internal partner teams.

See [Generic Resource Registry Design — §10](../generic-resource-registry-design.md) for the rationale.

#### Required Action

1. Deploy v1.0.0 with a **fresh, empty database**
2. Do **not** attempt to connect v1.0.0 to a v0.2.0 database
3. Re-register all resources (clusters, node pools) via the API after deployment
4. Existing cluster state is lost — adapters must re-provision or re-discover

> **If you don't act:** The API fails to start or exhibits undefined behavior when encountering the old schema.

---

### 4.2 Descriptor-driven delete model

#### What Changed

The delete behavior for child resources is now driven by the entity descriptor's `on_parent_delete` policy (`restrict` or `cascade`), not by a caller-supplied `?cascade=true` query parameter. Different child types of the same parent can have different policies.

#### Required Action

Remove any `?cascade=true` query parameters from delete API calls. Configure the delete policy in the entity descriptor instead:

**Before (v0.2.0):**

```bash
curl -X DELETE "${API_URL}/api/hyperfleet/v1/clusters/${ID}?cascade=true"
```

**After (v1.0.0):**

```bash
# Delete policy is configured per-entity-type, not per-request
curl -X DELETE "${API_URL}/api/hyperfleet/v1/clusters/${ID}"
# Returns 409 Conflict if restrict-policy children exist
# Cascades automatically for cascade-policy children
```

> **If you don't act:** The query parameter is ignored. Deletion behavior is determined by the entity descriptor.

---

### 4.3 Unknown config fields cause startup errors

#### What Changed

Any unexpected property in a configuration file triggers an error at load time. This prevents silent misconfiguration from typos or leftover v0.2.0 fields.

#### Required Action

Before deploying v1.0.0, remove any configuration properties that are not part of the v1.0.0 schema. Test your configuration against the v1.0.0 binary before deploying.

> **If you don't act:** The application refuses to start with an error message identifying the unexpected field.

---

### 4.4 No runtime config reloading

#### What Changed

HyperFleet applications do not support runtime configuration reloading. All configuration changes require an application restart.

#### Required Action

**No action required** — this is informational. Plan for pod restarts when changing configuration.

---

## 5. New Features (Informational)

These features are new in v1.0.0 and do not require action from existing operators or adapters:

| Feature | Description | Reference |
|---------|-------------|-----------|
| **CEL Condition Mapping** | Operators can declaratively copy/transform adapter conditions into customer-visible resource conditions | [Condition Mapping Design](../../components/api-service/condition-mapping-design.md) |
| **Generic Resource Registry** | New entity types can be added by config-only changes (no Go code required) | [Generic Resource Registry Design](../generic-resource-registry-design.md) |
| **Resource References** | Non-ownership associations between resources with cardinality constraints and deletion restriction | [Generic Resource Registry Design — §9](../generic-resource-registry-design.md) |
| **Force Delete** | Operators can force-delete resources stuck in `Finalizing` state via `POST /{id}/force-delete` | [Force Deletion Design](../force-deletion-design.md) |
| **Automatic Hard Delete** | When all adapters report `Finalized=True`, the API hard-deletes records atomically within the status PUT transaction | [Hard Delete Design](../../components/api-service/hard-delete-design.md) |
| **Sentinel `condition()` CEL Helper** | Decision expressions can use `condition(name)` to access any condition type's fields | [Sentinel Design](../../components/sentinel/sentinel.md) |
| **OpenTelemetry Tracing** | When enabled, all components use OpenTelemetry with W3C Trace Context propagation | [Tracing Standard](../../standards/tracing.md) |
